# 📂 Agent Trajectories & Multi-Agent Execution Audit Logs
### Frontier Engineering Challenge 2026 — micro1

This directory contains curated, structured execution trajectories (`.jsonl`, `.json`, and human-readable `.md`) generated by the autonomous multi-agent FSM squad and the baseline evaluator.

---

## 🧭 Navigation & Key Architectural Trajectory Highlights

For quick inspection by the micro1 judging panel, the following representative trajectories showcase key architectural decisions, tool interactions, and failure modes:

| Highlighted Scenario | Archetype / Architectural Dilemma | Key Tool & Agent Telemetry | Primary Trajectory File |
| :--- | :--- | :--- | :--- |
| **Case 10 (🔥 The Challenging Case)** | **The Reverse Lock Order Distributed Deadlock** (`encode/databases`) | `LoadSimulator` detects opposing concurrent coroutines entering circular deadlock (45% error rate, 15,000ms latency). `CriticAgent` rejects candidate (25.2/100). | [`advanced_case_10_cand_10`](./advanced_case_10_cand_10_70233c54.md) |
| **Case 01** | **In-Memory Cache State Drift across Multi-Worker Pods** (`encode/starlette`) | `ContextInspector` flags `@lru_cache` usage across ASGI worker pods; `LoadSimulator` detects cache inconsistency. | [`advanced_case_01_cand_01`](./advanced_case_01_cand_01_c67e205b.md) |
| **Case 02** | **RAM Exhaustion (.all()) vs Streaming Queries** (`sqlalchemy/sqlalchemy`) | `LoadSimulator` tracks 1024MB memory peak against 256MB SLA ceiling; flags OOM risk under volume. | [`advanced_case_02_cand_02`](./advanced_case_02_cand_02_e5266fd4.md) |
| **Case 04** | **Async Event Loop Starvation via Sync HTTP** (`encode/httpx`) | `LoadSimulator` flags synchronous `requests.post` inside `async def`, causing 90% throughput collapse. | [`advanced_case_04_cand_04`](./advanced_case_04_cand_04_f07db5ed.md) |
| **Case 08** | **CWE-89 SQL Injection via String Interpolation** (`pallets/werkzeug`) | `CodeVerifierAgent` detects raw SQL formatting. `SeniorEngineeringCriticAgent` overrides score to 30.0/100 (REJECT). | [`advanced_case_08_cand_08`](./advanced_case_08_cand_08_4ca33125.md) |
| **Case 14 (🌟 Senior Hire)** | **Graceful Shutdown & Signal Draining** (`encode/uvicorn`) | Candidate implements zero-downtime drain coordinator. System awards **94.9/100 (STRONG_HIRE)**. | [`advanced_case_14_cand_14`](./advanced_case_14_cand_14_22a5feb5.md) |

---

## 📊 Trajectory File Formats

Each execution produces three complementary formats:
1. **`.jsonl` (Crash-Safe Streaming Log):** Line-by-line JSON objects emitted in real-time as each FSM state transition and tool response occurs.
2. **`.json` (Complete Hierarchical Graph):** Complete JSON schema including total duration, token consumption, cost in USD, and structured step inputs/outputs.
3. **`.md` (Executive Evaluation Dossier):** Human-readable Markdown summary with a tabular timeline and detailed tool inspection payloads.

---

## 💻 Terminal Interactive Trajectory Viewer

To interactively inspect any trajectory with syntax highlighting and rich terminal formatting:

```bash
python3 -m src.tracing.viewer
```
